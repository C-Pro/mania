{
    "name": "entities",
    "version": "1.0.0",
    "description": "",
    "main": "import.js",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "author": "",
    "license": "ISC",
    "dependencies": {
        "actions-on-google": "^3.0.0",
        "dialogflow": "^4.0.3"
    },
    "devDependencies": {
        "eslint": "^5.6.0",
        "eslint-config-google": "^0.10.0"
    },
    "eslintConfig": {
        "extends": "google",
        "rules": {
            "max-len": [
                "error",
                {
                    "code": 100,
                    "ignoreUrls": true
                }
            ]
        }
    }
}
